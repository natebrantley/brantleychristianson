# Broker headshots

Place broker headshot images here. Filenames should match the `image` paths in `src/data/agents.json` (e.g. `corey.jpg`, `nate.jpg`, `ashley.jpg`).

Served at `/media/img/brokers/{filename}`.
